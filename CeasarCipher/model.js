// The text that the user inputs. Will be transformed using a cipher function.
let plainText = '';

// Encrypt function takes plainText and splits it into an array.
let plainTextArrau = [];


// Stores the plain text after it has been encrypted.
let encryptedText = '';


// The key that the cipher uses to encrypt it (can also be used to decrypt it?). 
// Should probably not store it her. Will fix later.
// for now the key will just be an integer
let key = ""; 



